#include <stdio.h>
int main()
{
    int n;
    printf("Enter an integer:");
    scanf("%d",&n);
    if(n>0)
    printf("the number %d is positive",n);
    else
    printf("the number %d is negative",n);
    }
